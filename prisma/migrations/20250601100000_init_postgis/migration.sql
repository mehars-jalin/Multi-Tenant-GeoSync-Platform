-- CreateExtension
CREATE EXTENSION IF NOT EXISTS postgis;

-- Placeholder table for Prisma client generation
CREATE TABLE "_schema_anchor" (
    "id" INTEGER NOT NULL DEFAULT 1,

    CONSTRAINT "_schema_anchor_pkey" PRIMARY KEY ("id")
);
